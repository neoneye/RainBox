# Research-only dependencies, kept out of the main requirements.txt so the
# research pipeline's dependency tail can't entangle the main app's resolver.
# Install into the SAME venv as the app (research imports db and llm):
#
#     venv/bin/pip install -r research/requirements.txt
#
# These modules are imported lazily inside research/ only — the main app
# never loads them.

# Direct dependencies
trafilatura==2.1.0                           # main-text extraction from fetched pages
ddgs==9.14.4                                 # DuckDuckGo search (keyless)

# Transitive (pinned for supply-chain safety; regenerate with `pip freeze`)
babel==2.18.0
brotli==1.2.0
courlan==1.4.0
dateparser==1.4.1
fake-useragent==2.2.0
h2==4.3.0
hpack==4.2.0
htmldate==1.10.0
hyperframe==6.1.0
justext==3.0.2
lxml==6.1.1
lxml_html_clean==0.4.5
primp==1.3.1
pytz==2026.2
socksio==1.0.0
tld==0.13.2
tzlocal==5.4.4
